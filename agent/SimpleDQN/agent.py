


class DQNAgent:

    def get_next_action(self, cur_state):

    def store_exp(self, new_state, reward, action):

    def train():
        for self.step in xrange(start, end):
            # Get env state
            self.cur_state =
            # Act on the environment
            self.cur_action = 
            self.next_state = 
            # Save experience
                        
            # Train Q-network 
            
            # Update target network

    def test():

